First, fetch props.currentUser's users document/data from the DB. Then get their "projects" list. Then, fetch the data of those projects from the DB and compile them into an array, then setProjects(array)